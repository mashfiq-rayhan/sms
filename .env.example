# Postgres connection string used by Prisma.
# Matches the docker-compose.yml service (postgres/postgres/sms on localhost:55432 —
a non-default port is used to avoid clashing with any other Postgres already
running on your machine, natively or in another project's Docker container).
DATABASE_URL="postgresql://postgres:postgres@localhost:55432/sms?schema=public"
